# Experiment Report — {{session_name}}

> Auto-generated by `/interpret-experiment` on {{date}}. Read alongside `plan/RESEARCH_OBJECTIVE.md` and `plan/PLAN.md`.

## 1. Objective

*(One sentence, lifted verbatim from `plan/RESEARCH_OBJECTIVE.md`.)*

{{objective}}

## 2. Headline finding

*(One paragraph. Direct answer to the objective. Lead with the strongest single piece of evidence and cite its artifact path inline.)*

{{headline}}

## 3. Verdict against success criteria

*(One row per criterion in `plan/RESEARCH_OBJECTIVE.md`. If the plan declared `(not specified)`, write that here verbatim.)*

| Criterion | Result | Evidence |
|---|---|---|
| {{criterion}} | met / unmet / inconclusive | `artifacts/.../results.json` |

## 4. Verdict against hypotheses

*(One row per hypothesis. If the plan declared `(not specified)`, write that here verbatim. For hypotheses with a falsification condition, evaluate the condition explicitly and quote the numbers.)*

| Hypothesis | Outcome | Evidence |
|---|---|---|
| {{hypothesis}} | confirmed / falsified / inconclusive | `artifacts/.../results.json` |

## 5. What ran

*(Lifted from `run/<runner>_resolved.yaml` and the session-local code inventory. Include the diff against PLAN.md §F's expected artifact tree.)*

- **Task / model / variant**: ...
- **Sweep axes** (if any): ...
- **Shipped analyses used**: ...
- **Session-local analyses used** (with paths under `code/analyses/`): ...
- **Session-local methods used** (with paths under `code/methods/`): ...
- **Diff vs. planned tree**: unplanned runs / missing planned runs / unchanged.

## 6. Per-analysis findings

*(One subsection per analysis that produced artifacts — both shipped and session-local. Discovered dynamically; not a fixed list. For each: lift the research question from `plan/PLAN.md` §D (or from the analysis's `README.md` for session-local ones), state the key numbers, embed the headline figure, give 1–3 sentences of interpretation, and link to the raw artifact directory.)*

### 6.x {{analysis_name}}

- **Research question**: {{question}}
- **Key numbers**: {{numbers}}
- ![{{analysis_name}}]({{figures/analysis_name_headline.pdf}})
- **Interpretation**: {{1–3 sentences}}
- **Artifacts**: `artifacts/.../{{analysis_name}}/`

## 7. Paper comparison

*(Only populated for replication sessions — when `plan/paper_context.md` exists. Otherwise render `*(not a replication session)*` here verbatim and skip the table. For replication sessions: header includes the paper citation lifted from `paper_context.md`; one row per `(component, paper_metric, paper_value)` tuple in the "Reported findings" table; "Our value" cites the artifact path; `Match?` is `yes` / `partial` / `no`; discrepancy category is `methodology` / `model` / `dataset` / `genuine`.)*

**Paper:** {{citation}}

| Component / variable | Paper's metric | Paper's value | Our value | Match? | Discrepancy category |
|---|---|---|---|---|---|
| {{component}} | {{metric}} | {{paper_value}} | {{our_value}} (`artifacts/.../results.json`) | yes / partial / no | methodology / model / dataset / genuine |

Discrepancies (1–2 sentences each, naming the category):
- {{discrepancy_1}}
- {{discrepancy_2}}

## 8. Cross-run / cross-session comparison

*(Only present if a sweep ran or cross-session reuse was declared. Comparison axes are derived from `plan/PLAN.md` §F. Include comparison tables and figures inline. If cross-session, cite the other session's `result/REPORT.md`.)*

{{comparison_section}}

## 9. Caveats & open questions

*(Always present. Use bullets.)*

- Missing or partial artifacts: ...
- Failed pre-flight gates that may have polluted downstream results: ...
- Plan/run mismatches: ...
- Open questions surfaced by the findings (candidates for follow-up sessions): ...

## 10. Suggested next steps

*(Concrete, plan-aware. Examples: "session-local method `xyz` warrants promotion to `causalab/methods/`", "rerun with `subspace.k_features=32` to disambiguate the saturation pattern", "run `/replicate-paper` against {paper} to validate the geometric claim".*

*For replication sessions, append the "Suggested extensions" bullets from `plan/paper_context.md` here, prefixed with **`From the paper:`** so they remain distinguishable from session-derived next steps.)*

- {{next_step_1}}
- {{next_step_2}}
- **From the paper:** {{paper_extension_1}}
- **From the paper:** {{paper_extension_2}}

## 11. Provenance

- Plan: `plan/RESEARCH_OBJECTIVE.md`, `plan/PLAN.md`
- Paper context (replication only): `plan/paper_context.md`
- Resolved config: `run/{{runner}}_resolved.yaml`
- Run log: `run/run.log`
- Cross-session references: {{list of agent_logs/{other_session}/result/REPORT.md, or "none"}}
